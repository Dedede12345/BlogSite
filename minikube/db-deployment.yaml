apiVersion: apps/v1
kind: Deployment
metadata:
  name: db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      volumes:
        - name: db-files
          persistentVolumeClaim:
            claimName: db-pvc
      containers:
        - name: nginx
          image: postgres
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-files
          env:
            - name: POSTGRES_USER
              value: dev
            - name: POSTGRES_PASSWORD
              value: password
            - name: POSTGRES_DB
              value: blogdb

          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: db
spec:
  selector:
    app: db
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432